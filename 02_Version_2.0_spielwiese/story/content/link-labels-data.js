/**
 * link-labels-data.js – Anzeigetexte für Links (Sidebar, Signatur, ATS). URLs kommen aus CV_PROFILE.profile.links.
 * Nach dem Einbinden: window.LINK_LABELS_DATA
 */
(function () {
  "use strict";

  window.LINK_LABELS_DATA = {
    linkedin: {
      arrow: "↗ LinkedIn",
      label: "LinkedIn"
    },
    github: {
      arrow: "↗ GitHub",
      label: "GitHub"
    }
  };
})();
